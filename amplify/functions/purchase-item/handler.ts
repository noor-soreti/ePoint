// import { Schema } from '../../data/resource';

// export const handler: Schema["purchaseItem"]["functionHandler"]  = async (event) => {
//   // your function code goes here
//   const { businessId, description, itemId, name, price } = event.arguments
//   return `BusinessId: ${businessId}\nDescription: ${description}\nItemId: ${itemId}\nName: ${name}\nPrice: ${price}`;
// };